name: mini-lakehouse

x-spark-env: &spark-env
  AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
  AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
  AWS_REGION: ${AWS_REGION}
  S3_ENDPOINT: ${S3_ENDPOINT}
  HIVE_METASTORE_URI: ${HIVE_METASTORE_URI}
  ICEBERG_WAREHOUSE: ${ICEBERG_WAREHOUSE}
  SPARK_MASTER_URL: ${SPARK_MASTER_URL}

services:
  # ============================================================
  # 1. POSTGRES - backend for Airflow, Superset, Hive, + source
  # ============================================================
  postgres:
    image: postgres:14
    container_name: postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      HIVE_DB: ${HIVE_DB}
      HIVE_DB_USER: ${HIVE_DB_USER}
      HIVE_DB_PASSWORD: ${HIVE_DB_PASSWORD}
      SUPERSET_DB: ${SUPERSET_DB}
      SOURCE_DB: ${SOURCE_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init-multiple-dbs.sh:/docker-entrypoint-initdb.d/init-multiple-dbs.sh
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - lakehouse-net

  # ============================================================
  # 2. MINIO - S3-compatible object storage
  # ============================================================
  minio:
    image: minio/minio:RELEASE.2024-06-13T22-53-53Z
    container_name: minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - minio-data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - lakehouse-net

  # auto-create buckets then exit
  minio-init:
    image: minio/mc:RELEASE.2024-06-12T14-34-03Z
    container_name: minio-init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD};
      mc mb -p local/raw local/bronze local/silver local/gold local/platinum local/warehouse local/meta;
      mc anonymous set download local/raw;
      echo 'Buckets created'; exit 0;
      "
    networks:
      - lakehouse-net

  # ============================================================
  # 3. HIVE METASTORE - Iceberg catalog
  # ============================================================
  hive-metastore:
    build:
      context: ./docker/hive
    container_name: hive-metastore
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      SERVICE_NAME: metastore
      DB_DRIVER: postgres
      HIVE_DB: ${HIVE_DB}
      HIVE_DB_USER: ${HIVE_DB_USER}
      HIVE_DB_PASSWORD: ${HIVE_DB_PASSWORD}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
    ports:
      - "9083:9083"
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'cat < /dev/null > /dev/tcp/localhost/9083'"]
      interval: 15s
      timeout: 5s
      retries: 10
    networks:
      - lakehouse-net

  # ============================================================
  # 4. SPARK - master + worker (custom image w/ Iceberg jars)
  # ============================================================
  spark-master:
    build:
      context: ./docker/spark
    container_name: spark-master
    environment:
      <<: *spark-env
      SPARK_MODE: master
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master
    ports:
      - "7077:7077"
      - "8080:8080"
    volumes:
      - ./pipeline:/opt/pipeline
      - ./dataset:/opt/dataset
    networks:
      - lakehouse-net

  spark-worker:
    build:
      context: ./docker/spark
    container_name: spark-worker
    depends_on:
      - spark-master
    environment:
      <<: *spark-env
      SPARK_MODE: worker
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077
    ports:
      - "8081:8081"
    volumes:
      - ./pipeline:/opt/pipeline
      - ./dataset:/opt/dataset
    networks:
      - lakehouse-net

  # ============================================================
  # 5. TRINO - SQL query engine over Iceberg
  # ============================================================
  trino:
    image: trinodb/trino:440
    container_name: trino
    depends_on:
      - hive-metastore
    ports:
      - "8090:8080"
    volumes:
      - ./docker/trino/catalog:/etc/trino/catalog
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/v1/info"]
      interval: 15s
      timeout: 10s
      retries: 10
    networks:
      - lakehouse-net

  # ============================================================
  # 6. AIRFLOW - orchestration (webserver + scheduler)
  # ============================================================
  airflow-webserver:
    build:
      context: ./docker/airflow
    container_name: airflow-webserver
    depends_on:
      postgres:
        condition: service_healthy
    environment: &airflow-common-env
      <<: *spark-env
      AIRFLOW__CORE__EXECUTOR: ${AIRFLOW__CORE__EXECUTOR}
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "false"
      _AIRFLOW_WWW_USER_USERNAME: ${_AIRFLOW_WWW_USER_USERNAME}
      _AIRFLOW_WWW_USER_PASSWORD: ${_AIRFLOW_WWW_USER_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      SOURCE_DB: ${SOURCE_DB}
    volumes: &airflow-volumes
      - ./dags:/opt/airflow/dags
      - ./pipeline:/opt/pipeline
      - ./dataset:/opt/dataset
      - ./tests:/opt/tests
      - airflow-logs:/opt/airflow/logs
    ports:
      - "8085:8080"
    command: >
      bash -c "airflow db migrate &&
               airflow users create --username ${_AIRFLOW_WWW_USER_USERNAME} --password ${_AIRFLOW_WWW_USER_PASSWORD} --firstname admin --lastname admin --role Admin --email admin@example.com || true &&
               airflow webserver"
    networks:
      - lakehouse-net

  airflow-scheduler:
    build:
      context: ./docker/airflow
    container_name: airflow-scheduler
    depends_on:
      airflow-webserver:
        condition: service_started
    environment:
      <<: *airflow-common-env
    volumes: *airflow-volumes
    command: >
      bash -c "airflow db migrate && airflow scheduler"
    networks:
      - lakehouse-net

  # ============================================================
  # 7. SUPERSET - BI dashboard
  # ============================================================
  superset:
    build:
      context: ./docker/superset
    container_name: superset
    depends_on:
      postgres:
        condition: service_healthy
      trino:
        condition: service_started
    environment:
      SUPERSET_SECRET_KEY: ${SUPERSET_SECRET_KEY}
      SUPERSET_ADMIN: ${SUPERSET_ADMIN}
      SUPERSET_ADMIN_PASSWORD: ${SUPERSET_ADMIN_PASSWORD}
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${SUPERSET_DB}
    volumes:
      - ./docker/superset/superset_config.py:/app/pythonpath/superset_config.py
    ports:
      - "8088:8088"
    networks:
      - lakehouse-net

networks:
  lakehouse-net:
    driver: bridge

volumes:
  postgres-data:
  minio-data:
  airflow-logs:
